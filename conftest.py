# Presence of this file at the project root puts the root on sys.path for
# pytest (prepend import mode), so `import pawpal_system` resolves regardless
# of the working directory the test runner is launched from.
